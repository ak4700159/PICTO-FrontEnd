import 'package:flutter/cupertino.dart';

class UpscalingScreen extends StatelessWidget {
  const UpscalingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("업스일링"),
    );
  }
}
