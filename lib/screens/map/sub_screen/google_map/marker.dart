import 'package:flutter/cupertino.dart';

class PhotoMarker extends StatelessWidget {
  const PhotoMarker({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
