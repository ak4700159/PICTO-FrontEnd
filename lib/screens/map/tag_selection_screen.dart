import 'package:flutter/cupertino.dart';

class TagSelectionScreen extends StatelessWidget {
  const TagSelectionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('tag selection screen'),
    );
  }
}
