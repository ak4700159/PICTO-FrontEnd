import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class PasswdSettingScreen extends StatelessWidget {
  const PasswdSettingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('/passwd_setting_screen'),
      ),
    );
  }
}
