import 'package:flutter/cupertino.dart';

class PhotoBookScreen extends StatelessWidget {
  const PhotoBookScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("photo book screen"),
    );
  }
}
